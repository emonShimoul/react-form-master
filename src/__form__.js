/**
 * 1. use form tag and onSubmit handler with event(e)
 * form access: e.target.[name of the input field].value
 *
 * 2. controlled element: use individual field state for each input field
 *
 * 3. controlled element(2): one object in a state holding all the input fields value
 *
 * 4. uncontrolled element: use useRef hook to create a reference to the element and access value. ex: nameRef.current.value
 *
 * 5. use hook to handle states and submit and error. example: react hook form
 *
 * 6. reusable component
 *
 */
